# PROGRESS -- task-20260815-231659-commit---fix---wire-in-the-uncommitted-q

UMR-20260815-140654-0230

## Completed
- [x] Verified SPEC premise independently (not the false-premise pattern this time): confirmed via `git status --porcelain -uall` in the real live-server checkout `/opt/veridian/scripts` (NOT `/opt/veridian/repos/veridian-scripts`, which is clean) that `timer-manager.py` and `queue-manager.py` sit as real untracked files, never committed.
- [x] Reproduced bug (1) live: `systemctl --user list-timers --all --no-pager` shows all 6 real veridian-*.timer units currently stopped (NEXT/LEFT = bare `-`); `python3 timer-manager.py list` against that real state printed nothing at all (silent failure), confirming the `parts[4]` fixed-column-index bug.
- [x] Reproduced bug (2) live: `python3 queue-manager.py list --status queued` returned an empty table while real pre-dispatch backlog rows exist in `umr_tasks` (confirmed resource_governor.py already has `list_queue()`/`--list-queue` reading real `umr_tasks` rows).
- [x] Found resource_governor.py already exposes real queue-management primitives + CLI flags (`list_queue`, `stop_task`, `resume_task`, `delete_task`, `set_priority`, `move_up`, `move_down`, wired to `--list-queue`/`--stop-task`/`--resume-task`/`--delete-task`/`--set-priority`/`--move-up`/`--move-down`) -- not frozen (only dispatch_core.py is under the stop-work order, per memory).
- [x] Copied both uncommitted tools from the live checkout (`/opt/veridian/scripts/{timer,queue}-manager.py`) into this task's repo checkout.
- [x] Fixed `timer-manager.py`'s `list_timers()`: replaced fixed `parts[4]` column-index parsing with (a) an explicit `veridian-*.timer` unit-name pattern passed straight to `systemctl --user list-timers` (server-side filter, no client-side column guessing) and (b) printing matched lines verbatim instead of re-parsing columns to find the unit name.
- [x] Fixed `queue-manager.py`'s `list`: now delegates to `resource_governor.py --list-queue` (subprocess, parses its JSON) to surface real pre-dispatch `umr_tasks` rows, shown alongside post-dispatch `task.yaml` rows, each clearly labeled by source (`PRE-DISPATCH BACKLOG` vs `POST-DISPATCH TASKS`) so an empty one is never mistaken for an empty queue overall. Also added `stop-pending`/`resume-pending`/`priority-pending <umr_id>` subcommands delegating to `resource_governor.py --stop-task`/`--resume-task`/`--set-priority` so pre-dispatch rows are actionable, not just visible (preferred delegation fix over a warning label, per SPEC).
- [x] Added `tests/test_timer_manager.py` (5 tests) and `tests/test_queue_manager.py` (8 tests) -- all 13 pass. Also re-ran pre-existing `tests/test_pm_lifecycle.py` (25 pass) and `test_pm_sentinel_tick.py` (17 pass) to confirm the additive doc-only changes to those two files caused no regression.
- [x] Wired both tools as documented "RELATED REAL ADMIN COMMANDS" blocks into `pm-sentinel-tick.sh` (hourly PM oversight script) and `pm_lifecycle.py` (completion-lifecycle driver) -- additive references alongside their existing real `dispatch-owner-task.sh`/`resource_governor.py` calls; single-gateway dispatch path (`dispatch-owner-task.sh`) unchanged.
- [x] Re-reproduced both bugs as fixed against real live state on the server:
  - Bug (1): `python3 timer-manager.py list` now correctly prints all 6 real currently-stopped `veridian-*.timer` units with correct unit names (previously printed nothing).
  - Bug (2): `python3 queue-manager.py list --status running` correctly surfaces real non-empty `umr_tasks` rows (verified against `running` status, since the real `queued` backlog had drained to 0 by the time of this fix -- confirmed independently via a direct `resource_governor.py --list-queue --status queued` call showing the same real 0, not a tool bug).
- [ ] Open real PR, obtain real fresh AUDIT:PASS (independent, not self-certified -- dispatching a separate audit task via dispatch-owner-task.sh per this repo's own established pattern).
- [ ] Record completion to UMR-20260815-140654-0230.
