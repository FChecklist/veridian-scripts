# Task: Sweep veridian-scripts for real audited PRs ready to merge, resolve conflicts and merge

## Completed
- [x] Listed all 40 real open PRs on FChecklist/veridian-scripts via `gh api repos/.../pulls --paginate` (the higher-level `gh pr list --json` path was truncating output mid-stream in this environment -- worked around by using `gh api` directly).
- [x] Fetched real `mergeable`/`mergeable_state` for all 40 via `gh api repos/.../pulls/<n>`. Result: 9 are `mergeable=true`/`clean` (#378, #373, #372, #357, #349, #213, #196, #190, #62); the remaining 31 are `mergeable=false`/`dirty` (CONFLICTING).
- [x] Fetched real audit comments (issue comments matching /AUDIT/i) for all 40 PRs.
- [x] Cross-checked: **none** of the 9 clean/mergeable PRs currently have a passing audit matching HEAD (378 and 349 have a *FAIL* as their latest audit; 373/372/357/213/196/190/62 have **zero** audit comments at all). So the SPEC's "clean mergeable + passing audit -> merge directly" bucket is currently empty -- verified, not assumed.
- [x] Found 17 dirty/CONFLICTING PRs whose latest audit comment is a real `AUDIT: PASS`, and independently confirmed (via `gh api .../commits/<sha>` commit-committer-date vs. the audit comment's `created_at`) that in every case the audit was posted *after* the PR's current head commit -- i.e. the passing audit genuinely matches current HEAD, not a stale prior push: #385, #384, #371, #247, #244, #233, #232, #205, #200, #198, #118, #99, #93, #90, #71, #61, #60.
- [x] Confirmed none of these 17 are superseded by work already on `origin/main` (grepped main for `wiring_health_check.py`, `capability_semantic_search.py`, the PM-report section-16/17 helpers, `capability_graduation_log`, `check_registry_integrity` -- none exist yet on main).
- [x] Discovered a real, hard structural constraint before attempting any push: `pretooluse_worker_enforcement.py` mechanically blocks `git push` to any branch other than this worker's own assigned branch (`worker/task-20260814-183604-sweep-veridian-scripts-for-real-audited`), fail-closed, no override. Confirmed this is by design (matches this same repo's own PR #374/#376 rebase-task precedent in `/opt/veridian/scripts/progress/task-20260814-163404-*.md` and `task-20260814-170148-*.md`): a worker cannot push directly onto a *different* PR's own head branch, even to resolve a conflict on an otherwise-approved PR. The established, already-used pattern for this shape of work is: rebase the target PR's real commits (preserved, not squashed) onto current `origin/main` locally, push the result to *this worker's own* branch, open a new PR from it that supersedes the original, merge that, then close the original PR with a comment linking to the real merge.
- [x] Applied that pattern to the 9 **docs-only** PASS-audited/CONFLICTING PRs (each touches only `PROGRESS.md` + one brand-new, uniquely-named markdown file -- verified via `git diff origin/main...pr-N --stat` before and after rebase): #233, #244, #90, #93, #71, #60, #99, #118, #371. All 9 rebased cleanly with the only conflict being the shared `PROGRESS.md` header/status-line collision, resolved by keeping each commit's own `PROGRESS.md` content (the established repo convention, cited by name in PR #198's own AUDIT:PASS finding). Stacked all 9 rebased commits (in oldest-first order) onto this worker's own branch, preserving every original commit hash's message/authorship via `git rebase`/cherry-pick (not squashed).

## Remaining
- [ ] Push consolidated branch, open one superseding PR referencing all 9 original PR numbers + their real AUDIT:PASS comments, merge it, then close #233/#244/#90/#93/#71/#60/#99/#118/#371 individually with a comment linking to the real merge commit.
- [ ] Assess the remaining 8 PASS-audited/CONFLICTING PRs that contain real code (not docs-only): #385, #384, #247, #232, #205, #200, #198, #61. These are higher-risk to batch (real `.py` changes, some touching the same large shared files as each other and as recent main history) -- each needs its own rebase + its own real test re-run, and per the same one-branch-per-worker constraint, each realistically needs its own dedicated follow-up worker task (matching the PR #374/#376 precedent) rather than being combined into this session's single branch/PR. Will attempt at least the lowest-risk ones directly if time/budget allow; otherwise will report them precisely as real, verified, ready-to-merge-pending-rebase candidates for dedicated follow-up dispatch.
- [ ] Final report: exact PR numbers + real state + which were actually merged, with evidence (commit SHAs, PR links).
- [ ] `agent_work_briefing.py record-completion` with the real summary.
