# PROGRESS -- task-20260814-183228-build-single-command-full-lifecycle-orch

## Completed
- [x] Deterministic briefing checked before writing: wiring_registry row `dispatch_event-owner-task-20260814-182157-675520` is this task's own dispatch event (not a prior duplicate build). capability_registry rows `single_deterministic_orchestrator_pipeline` / `umr_5767_issue_resolution_matrix` are resource_governor.py's `run_tick()` 12-step *deterministic dispatch-decision* pipeline (systemd-timer-driven, zero-AI, resolves the 619 already-resolvable UMR-5767 issues) -- a genuinely different, orthogonal thing from this task's PM-facing, manually-invoked, single-task full-lifecycle command. Confirmed no duplication; not rebuilt.
- [x] Confirmed live dependency state (`git log --oneline`): of the 4 sibling UMRs this task builds on, only UMR-20260814-180439-7462 (real file-attachment intake, PR #383) was merged on this checkout's origin/main. Token-usage instrumentation, search-results cache, and completion-verification-against-gh (UMR-181107-6bd6) were NOT yet merged -- per SPEC instruction, integrated gracefully: `--attach` wired straight through to `task-gateway.py submit`'s real `--attach`; gh-based completion verification reuses `reconcile_owner_dispatch_status.py`'s own existing, tested primitives (`_task_dir_from_unit`/`_read_task_yaml`/`_fetch_prs`/`_audit_verdict`) rather than re-implementing the not-yet-merged UMR-181107-6bd6.
- [x] Built `pm_lifecycle.py` (new script, root of this repo) -- the real single-command full-lifecycle orchestrator:
  1. `submit_classify()` -- calls `task-gateway.py submit` (search/dedup/capability decision).
  2. `check_deterministic_path()` -- honest software-only gate; real allow-list (`SAFE_DETERMINISTIC_EXECUTORS`, empty by default) so it never blind-executes an arbitrary matched capability row.
  3. `build_tightened_prompt()` + `dispatch_task()` -- assembles a labeled (`## OBJECTIVE/SCOPE/SUCCESS_CRITERIA/EXPECTED_OUTPUT/KNOWN_CONTEXT/COMPLEXITY_TIER`) prompt matching `tight_task_validation.py`'s own validated shape, dispatches via the existing `dispatch-owner-task.sh` front door.
  4. `poll_until_terminal()` -- polls `resource_governor.py --query-umr --umr-id` at a sane interval until status leaves `UMR_ACTIVE_STATUSES`, bounded by `--poll-timeout`.
  5. `independent_verify()` -- re-derives real PR + AUDIT:PASS/FAIL freshness-vs-current-head via `reconcile_owner_dispatch_status.py`'s own functions; never trusts the register's own status label.
  6. `should_retry_fix()` / `dispatch_audit_fix()` / `verify_with_retries()` -- on a real fresh AUDIT:FAIL, dispatches one real fix citing the specific finding (same pattern `pm-sentinel-tick.sh`'s `dispatch_gap()` already uses manually), re-verifies; capped at `--max-fix-retries` (default 2), surfaces to the PM rather than looping forever.
  7. `merge_and_reverify()` -- once AUDIT:PASS is fresh and the PR is real/OPEN, merges via `gh pr merge --merge` (the same real call `supervisor-entrypoint.sh`'s own independent-review merge gate makes -- this script IS that independent reviewer at that point, consistent with `pm-sentinel-tick.sh`'s AUDIT-REJECT fix #4 "never let a worker blindly merge its own PR" rule, not a violation of it), re-fetches PR state to confirm `state=MERGED` landed for real.
  8. `compute_six_columns()` / `record_six_columns()` -- computes the real, independently-derived 6-boolean status (COMPLETED/TESTED/AUDITED/INTEGRATED/WORKING/CERTIFIED, each with its own evidence dict, no self-certification) and writes it into the `umr_tasks` row's `metadata_json` via `superboss-register.py`'s own `update_umr_task()` under `_write_lock()` (real read-merge-write, same convention `reconcile_owner_dispatch_status.py`'s `apply_correction()` already uses).
- [x] Added `tests/test_pm_lifecycle.py` -- 12 real regression tests (all passing, `python3 -m pytest tests/test_pm_lifecycle.py -q` -> `12 passed`), covering (per this task's own SPEC requirement) at minimum:
  - polling-to-terminal-state logic: returns once status leaves the active set, honestly times out (never spins forever) when status stays active past `poll_timeout`, and handles a row that's never found.
  - audit-fail-retry logic: `fresh_audit_fail()`/`fresh_audit_pass()` correctly ignore a stale (pre-current-head) verdict; `should_retry_fix()` caps at `max_retries`; `verify_with_retries()` (the real end-to-end retry loop, extracted to its own injectable function) stops on a real fresh PASS, gives up (never loops forever) once the cap is hit, and stops immediately (no wasted retry) if a fix dispatch is itself refused/rejected.
  - plus structural checks on the tightened-prompt builder and the deterministic-execution allow-list (never auto-executes an unlisted capability).
- [x] Ran the pre-existing `tests/test_reconcile_owner_dispatch_status.py` alongside the new tests (`pytest tests/ -k "reconcile_owner_dispatch_status or pm_lifecycle"` -> `28 passed`) -- confirms the reused primitives still behave as this script expects them to.

- [x] Opened PR #389 (FChecklist/veridian-scripts) for `pm_lifecycle.py` + `tests/test_pm_lifecycle.py`.
- [x] Attempted the real end-to-end test run live (per SPEC's own "real done criteria") and, in doing so, found and fixed 2 more real bugs through live evidence rather than guessing:
  1. **Real bug found by actually running it**: `task-gateway.py submit`'s `duplicate_found` field is a *reuse-candidate* signal (`superboss-register.py check-duplicate` searches system_index/wiring_registry/knowledge_engine/capability_registry for an existing mechanism -- "search before building", per its own docstring), not an ask-level duplicate guard. The live run refused immediately with `duplicate_evidence` full of unrelated system_index rows (`worker-entrypoint.sh`, `doc-worker-entrypoint.sh`, ...) -- every real, well-scoped task that names existing files (as a well-informed task should) would have been wrongly refused. Fixed: no longer refuses on this signal (only surfaces it as `report.reuse_candidates_found`); refusal is left to `dispatch-owner-task.sh`'s own real, narrower duplicate guards (content-duplicate/target-identifier-duplicate), which were already being respected via `outcome="refused"`.
  2. (Found by code review before running, same session) `merge_and_reverify()`/`run_full_cycle()` used to treat any non-OPEN PR state as failure -- fixed to treat MERGED as a real success (the platform's own `supervisor-entrypoint.sh` may already have merged it for the tier-0/1/2 path), and added `needs_independent_audit()`/`dispatch_independent_audit()` for the tier-3/4 headless path, which has no automatic supervisor review at all.
- [x] All 15 regression tests still pass after both fixes (`python3 -m pytest tests/test_pm_lifecycle.py -q` -> `15 passed`). Fixes pushed to PR #389 (commits b1b655c, c4c2ad3).

## Remaining
- [ ] **Not yet done**: a real, successful, submission-to-verified-merged-PR run of `pm_lifecycle.py run` against a real test task -- the live attempt above was caught and refused by the bug in point 1 before reaching dispatch; this session's own remaining budget was exhausted fixing and re-verifying that bug rather than re-attempting the full live run (which involves a real ~15-20 min worker dispatch + potential independent-audit sub-dispatch). **Next session should re-run**, e.g.:
  `python3 pm_lifecycle.py run --title "..." --text "..." --repo veridian-scripts --tier 4 --poll-interval 20 --poll-timeout 1000 --max-fix-retries 2` (see this file's git history / the module docstring for a full worked example prompt), and record the real 6-column evidence from its printed JSON report into this file.
- [ ] `agent_work_briefing.py record-completion --umr-id UMR-20260814-182201-418d` once the real end-to-end run's evidence is in hand.
- [ ] Final commit+push of this progress file update once the real E2E run is recorded.

## Honest status note
The code deliverable (`pm_lifecycle.py`, the file this task's own objective names) is real, committed, pushed, and in an open PR (#389), with 15 passing regression tests covering the SPEC-required polling and audit-fail-retry logic. Two real, previously-undetected bugs were found and fixed via genuine live-running/code-review before this session's budget ran out. The one remaining SPEC requirement -- one real successful end-to-end run through the tool -- has NOT been completed yet and should not be assumed done.
