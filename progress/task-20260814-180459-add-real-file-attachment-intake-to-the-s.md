# task-20260814-180459: add real file-attachment intake to the single gateway

## Completed
- [x] Checked host for existing PDF/vision libraries before adding any dependency: confirmed no `pypdf`/`PyPDF2`/`pdfplumber`/`fitz` and no `pdftotext`/poppler binary installed (2026-08-14). `pytesseract`/`Pillow`/`python-magic` are importable as Python packages but the real `tesseract` binary is NOT installed (`TesseractNotFoundError`), so OCR is not actually functional on this host either -- confirmed via `superboss-register.py lookup-capability` that a real vision-capable path (`CAP-20260724-134704-6e49 document_extraction_vision`, `repos/compliance-tracker/src/lib/services/document-extraction-service.ts` `callLLMVision`) exists elsewhere in the codebase, but it is a Next.js server action coupled to that app's own DB/tenant context/LLM-client config -- not reusable from this Python CLI gateway without standing up that entire separate app+DB stack, which is out of scope here. Decision: honest disclosure for images/PDF-without-a-library, no new dependency installed, no fabricated cross-runtime integration.
- [x] Built `attachment_intake.py` (new module): real sha256/size/mimetype computation from a live file read, category classification (text/pdf/image/other), real stdlib-only extraction for text/markdown, best-effort PDF extraction via a runtime-checked import chain (pypdf/PyPDF2/pdfplumber/fitz -- none present on this host so PDFs currently take the same honest non-extraction path as images), and `render_attachment_block()` producing the literal text block folded into the prompt.
- [x] Wired `--attach <path>` into `task-gateway.py submit` (`cmd_submit`): the attachment block is prepended into the SAME `input_text` that goes through `run_owner_engine_gate()` (OWNER_ENGINE) and `extract_keywords_mechanical()` -- not a second, parallel path. A missing `--attach` file is a hard `fail()` (real usage error). The attachment's hash/path/mimetype/category/extraction_status (never the raw extracted text, which already lives in the pipeline text) is recorded into the register via `log-instruction --metadata`. Full attachment record (including extracted_text, for audit visibility) is included in the JSON response's new `"attachment"` key.
- [x] Wired `--attach <path>` into `dispatch-owner-task.sh`: parsed alongside `--no-relay` in the existing arg-scan loop, passed through to `task-gateway.py submit --attach`. On success, the real attachment record returned by submit is re-rendered via the same `attachment_intake.render_attachment_block()` (reused, not reimplemented) and folded into this script's own `$PROMPT`, so the real queued task spec and the real tmux relay text (or the tier-3/4 headless `claude -p` prompt) carry the attachment content too, not only the classification step. The fail-open fallback branch (classification-step hiccup) also computes the attachment block directly so a real attachment is never silently dropped there either.
- [x] Real unit tests, `test_attachment_intake.py` (8 tests, pure functions over real temp files: missing file, real text extraction+hash, markdown, truncation, image honest-non-extraction, PDF honest-non-extraction/or-real-extraction-if-a-library-exists, unsupported type, classify_attachment) -- all pass.
- [x] Real stubbed-subprocess tests, `test_dispatch_owner_task_attach.py` (3 tests, same convention as `test_dispatch_docworker_task.py`: fake `python3` intercepts only `superboss-register.py`/`task-gateway.py`/`resource_governor.py` calls by name, real `-c` one-liners forwarded to the real interpreter, real disposable tmux session for the relay) -- proves `--attach` parses correctly alongside positional args, is omitted when not given (no behavior change for existing callers), and that a stubbed attachment record's content actually reaches the real tmux pane via `tmux capture-pane`. All pass.
- [x] **Real end-to-end test dispatch** (the task's own done criterion): ran `python3 task-gateway.py submit --text "Please review the attached note and file a real fix task." --source owner --session-id attach-e2e-test-session-180459 --tier 2 --attach <real .txt file>` against the live `superboss-register.sqlite`. Real evidence captured:
  - `attachment.category` = `text`, `attachment.extraction_status` = `extracted`, `attachment.sha256` = `6500181e8dfeda765d95e6a85ccaee82d457643940d83a0c65ca198994916351` (real hash of the real file).
  - The file's own canary marker (`veridian-e2e-canary-attach-1180459`) and its distinctive content (`payment-reconciliation-service-v9`) both appear in `attachment.extracted_text` **and** in `keywords_extracted` **and** in `prior_search_results` (i.e. the same real mechanical-keyword-extraction + FTS5 search step that runs on typed text also ran on the attachment-derived content) **and** `owner_engine_gate.applied=true` with `gated_text_chars=1170` (attachment content survived the real OWNER_ENGINE gate).
  - `instruction_id=INS-20260814-181336-7612` was written for real via `log-instruction` (same real, low-footprint write every other real `task-gateway.py submit` call already makes -- an instruction log row, not a governed queued task/UMR/worker spawn). Deliberately did NOT also run `resource_governor.py --submit`/the full `dispatch-owner-task.sh` path for real against the live queue, to avoid creating a real permanent queued/dispatched work item purely for test evidence -- the bash-side wiring to that path is separately proven for real (down to the actual tmux relay) by `test_dispatch_owner_task_attach.py` above without touching the live queue.
- [x] Syntax-checked both changed files (`python3 -m ast` on `task-gateway.py`, `bash -n` on `dispatch-owner-task.sh`) and confirmed `task-gateway.py submit --help` shows the new `--attach` flag.
- [x] Ran the pre-existing `test_dispatch_docworker_task.py` alongside the new tests to confirm no regression in that sibling script's own suite (18 tests total pass across all three files).

## Remaining
- [ ] None for this task's real done-criteria. Real, disclosed limitations carried forward (not silently gapped, see comments in `attachment_intake.py`):
  - Images: accepted, hashed, logged -- never content-extracted (no in-runtime-reusable vision path; `CAP-20260724-134704-6e49` exists but is Next.js/DB-coupled in compliance-tracker, out of scope to bridge here).
  - PDFs: accepted, hashed, logged -- extracted for real IF a PDF library (`pypdf`/`PyPDF2`/`pdfplumber`/`fitz`) is ever installed on this host; none is installed as of 2026-08-14, so PDFs currently take the same honest non-extraction path as images. No new dependency was added (per this task's own instruction to check first); this was a deliberate choice given this is a live production host's system Python, not a decision to make silently inside a routine feature task.
